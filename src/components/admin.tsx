import { Link } from "react-router-dom"

const Admin = () => {
    return(
        <section>
            <div className="movie-container">
                <Link to="/movieseatbook" className="btn btn-primary">Back to Booking</Link>
            </div>
  
        </section>
    )
  }
  
  export default Admin